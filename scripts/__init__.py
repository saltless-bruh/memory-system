"""Self-written scripts for the SNP Memory System.

Marks `scripts/` as a package so tests can import modules here unambiguously
(e.g. `scripts.verify_addresses`) and mypy resolves a single module name.
The scripts remain runnable directly (``python scripts/<name>.py``).
"""
