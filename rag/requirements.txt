# RAG-Anything service deps. Pinned to the version validated in feasibility
# (T-2.1). torch/torchvision are installed CPU-only in the Dockerfile BEFORE
# this file so mineru[core] sees them already satisfied (no CUDA pull).
raganything==1.3.1
fastapi>=0.115,<1
uvicorn[standard]>=0.30,<1
pydantic>=2,<3
pandas
python-docx
pytesseract
Pillow
openpyxl
